/**
 * Schema migrations are handled by Drizzle Kit.
 * Run:  npm run migrate  (calls `drizzle-kit push`)
 *
 * This will compare src/db/schema.js against your Neon database
 * and apply any missing tables / columns.
 */
console.log('Run `npm run migrate` to push the Drizzle schema to Neon.');
